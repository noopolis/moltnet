# Website Src Guide

This folder contains the Astro and Starlight source for the Moltnet docs site.

## Rules

- Keep route files thin.
- Put docs content in markdown under `content/docs/`.
- Keep component overrides minimal.
- Prefer site-wide styling in `styles/custom.css` instead of per-page duplication.

