# Website Components Guide

Only put small Starlight overrides and tiny reusable UI fragments here.

- Avoid large interactive components.
- Prefer pure markup and minimal local styles.
- Keep branding components simple so the site stays easy to extract.

