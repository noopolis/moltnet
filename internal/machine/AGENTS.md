# internal/machine

Defines the packet-1 machine session runtime: bounded frame intake, deterministic
executor dispatch, correlation/lifetime/delivery state registries, cancel handling,
and one serialized newline-framed response writer.
